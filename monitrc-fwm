check host nodeapp-fwm with address 127.0.0.1
	start "/sbin/start nodeapp-fwm"
	stop "/sbin/stop nodeapp-fwm"
	if failed port 3000 protocol HTTP
		request /
		with timeout 5 seconds
		then restart
		if 5 restarts within 5 cycles then timeout
